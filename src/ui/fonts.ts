import { Rubik } from "next/font/google";
import { Vazirmatn } from "next/font/google";

export const rubik = Rubik({ subsets: [ "latin", "latin-ext", "arabic" ] })
export const vazir = Vazirmatn({ subsets: [ "latin", "latin-ext", "arabic" ] })